server:
  port: 80
  servlet:
    session:
      timeout: 1m  #设置session过期时间为1分钟
      cookie:
        name: JSESSIONID
spring:
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/jdbc?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8
    username: root
    password: '!QAZ2wsx'
    driver-class-name: com.mysql.cj.jdbc.Driver
  session:
    store-type: redis   #配置session存储位置
  #配置rides数据库
  redis:
    host: localhost
    port: 6379
#  security:
#    user:
#      name: admin
#      password: 123
#      roles: admin


liyiruo:
  security:
    authention:
      loginPage: /login/page #登录页面
      loginProcessingUrl: /login/form #登录表单提交URL
      usernameParameter: name #登录表单的用户名属性
      passwordParameter: pwd #登录表单的用户名属性
      imageCodeUrl: /code/image
      mobileCodeUrl: /code/mobile
      mobilePage: /mobile/page
      tokenValiditySeconds: 604800
      staticPaths:
        - /dist/**
        - /modules/**
        - /plugins/**
      loginType: REDIRECT  # 认证之后 响应的类型:JSON/REDIRECT
